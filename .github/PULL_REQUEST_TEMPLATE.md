## What changes and why?

Describe the problem and the resulting behavior. Link a related issue if there is one.

## Verification

- [ ] Relevant tests pass; behavior changes have regression coverage.
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass.
- [ ] CLI, schema, or workflow changes update the corresponding docs and agent instructions.

Include before/after images when changing annotation rendering. Identify any compatibility changes.
